class Question {
  String questionText;
  bool questionAnswer;

  Question(String q,
      bool a) // curly braces are used within to specify the parameter name
  {
    questionText = q;
    questionAnswer = a;
  }
}
